#include <stdio.h>
int main(){
 int num ,num1;
 scanf("%d %d",&num,&num1);
 if(num >num1){
    printf("%d is bigger than %d",num, num1);
 }else{
     printf("%d is bigger than %d",num1, num);

 }
 return 0;
}
